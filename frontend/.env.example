# URL base del backend. En desarrollo local con Vite (puerto 5173):
VITE_API_URL=http://localhost:3000

# Clave ANON de Supabase (opcional, solo si el frontend llega a necesitar cliente Supabase).
# ⚠️ NUNCA pongas aquí la SERVICE_ROLE_KEY.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
